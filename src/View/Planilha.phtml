<?php
// Controller deve ter mandado $planilhas com os dados vindos do banco
?>

<div class="container my-5">
  <h2 class="text-center mb-4">Minhas Planilhas</h2>

  <?php if (!empty($planilhas)): ?>
    <div class="list-group">
      <?php foreach ($planilhas as $planilha): ?>
        <a href="index.php?pagina=planilhaDetalhe&id=<?= $planilha['id'] ?>" 
           class="list-group-item list-group-item-action d-flex justify-content-between align-items-center">
          <span><?= htmlspecialchars($planilha['nome']) ?></span>
          <small class="text-muted"><?= htmlspecialchars($planilha['descricao']) ?></small>
        </a>
      <?php endforeach; ?>
    </div>
  <?php else: ?>
    <p class="text-center text-muted">Você ainda não tem planilhas cadastradas.</p>
  <?php endif; ?>

  <div class="text-center mt-4">
    <a href="index.php?pagina=criarPlanilha" class="btn btn-primary">+ Criar Nova Planilha</a>
  </div>
</div>
