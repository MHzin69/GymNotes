<?php include __DIR__ . '/../partials/header.php'; ?> <!-- se você já tiver um header -->

<div class="container py-4">
  <h1 class="mb-4">Biblioteca de Exercícios</h1>

  <?php foreach ($grupos as $grupo => $exercicios): ?>
    <h3 class="mt-4"><?= htmlspecialchars($grupo) ?></h3>
    <div class="row">
      <?php foreach ($exercicios as $ex): ?>
        <div class="col-md-4 mb-3">
          <div class="card h-100">
            <?php if(!empty($ex['imagem'])): ?>
              <img src="<?= htmlspecialchars($ex['imagem']) ?>"
                   class="card-img-top"
                   alt="<?= htmlspecialchars($ex['nome']) ?>">
            <?php endif; ?>
            <div class="card-body">
              <h5 class="card-title"><?= htmlspecialchars($ex['nome']) ?></h5>
              <p class="card-text"><?= htmlspecialchars($ex['descricao']) ?></p>
              <!-- No futuro: botão para adicionar na planilha -->
              <a href="#" class="btn btn-primary btn-sm disabled">Adicionar</a>
            </div>
          </div>
        </div>
      <?php endforeach; ?>
    </div>
  <?php endforeach; ?>
</div>

<?php include __DIR__ . '/../partials/footer.php'; ?>
